// import { fromJS } from 'immutable';
// import { selectDeleteTransactionButtonDomain } from '../selectors';

describe('selectDeleteTransactionButtonDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
